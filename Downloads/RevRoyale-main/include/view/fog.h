#pragma once

// ---------------------------------------------------------------------------
// Developer fog-of-war toggle.
// Set fogEnabled = false (or press F in-game) to reveal the full map.
// ---------------------------------------------------------------------------
namespace Debug {
    inline bool fogEnabled = true;
}
